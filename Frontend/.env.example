# Local development (default: http://localhost:3000 if unset)
VITE_API_URL=http://localhost:3000

# Production — set in Vercel → Environment Variables, then REDEPLOY
# Use your Render URL only (no trailing slash, no /api suffix):
# VITE_API_URL=https://ai-resume-analyzer-2-xl5y.onrender.com
